Here are the Git commands which are being covered:

git config --> configure the file
git init --> initialize the git repository
git clone
git add
git commit
git diff
git reset
git status
git rm
git log
git show
git tag
git branch
git checkout
git merge
git remote
git push --> to push the data in the repository
git pull--> pull the data
git stash
git add --> new line added








































Git Init command
This command is used to create a local repository.

Syntax

$ git init Demo  
The init command will initialize an empty repository. See the below screenshot.